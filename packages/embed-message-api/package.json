{
  "name": "@wealthsweet/embed-message-api",
  "version": "0.0.1",
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "5.6.3"
  },
  "files": [
    "dist/**"
  ],
  "main": "dist/index.cjs",
  "module": "dist/index.js",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "repository": "https://github.com/wealthsweet/open-source",
  "scripts": {
    "build": "tsup",
    "clean": "rm -r dist coverage .tsup || true",
    "clean:full": "rm -r dist coverage node_modules .turbo .tsup || true",
    "lint": "eslint . && prettier -c src/"
  },
  "type": "module",
  "types": "dist/index.d.ts"
}
